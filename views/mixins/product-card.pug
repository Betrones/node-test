mixin product-card(product)
	.col-md-3
		.card
			img(class="card-img-top" src=product.image)
			.card-body.text-ceneter
				h5.card-title= product.name
				p.card-text= product.price
				a(class="btn btn-primary" href="") Купить